import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input:{
        backgroundColor: '#d4d4d4',
        borderColor: 'black',
        borderWidth: 2,
        width: 240,
        textAlign: 'center',
        borderRadius: 10,
        fontSize: 16,
        fontWeight: 'bold'
    }
});